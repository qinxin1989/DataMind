# 统一 Agent 编排层实施计划

## 目标

- [x] 建立统一的业务 Agent Profile 机制
- [x] 支持按业务场景选择可用 skills / SQL / MCP 能力
- [x] 将现有数据问答与爬虫助手接入同一套编排层
- [x] 为后续知识库问答统一入口预留扩展点
- [x] 在前端提供统一智能助手入口，体现按业务切换 Agent 的模块化体验
- [x] 完成测试与编译验证

## 范围

- [x] 新增统一业务路由与 Agent Profile 定义
- [x] 为 `runAgentLoop` 增加技能白名单和业务系统提示词
- [x] 将 `ai-qa` 的流式入口切到统一编排层
- [x] 将 `ai-crawler-assistant` 的对话入口切到统一编排层
- [x] 新增 `/ai/assistant` 前端工作台，并保留旧页面兼容入口

## 非目标

- [x] 本轮不删除旧接口，只先兼容收口
- [x] 本轮不重写知识库检索内核，只保留后续接入位
- [x] 本轮不下线旧版 `AI问答 / 知识库 / 采集助手` 页面
