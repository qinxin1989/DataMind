# ========================================
# AI 数据平台 - 环境配置模板
# ========================================
#
# 安全部署流程：
# 1. 复制此文件为 .env 并填写真实配置
# 2. 运行 npm run encrypt-env 加密敏感配置
# 3. 生成 .env.encrypted 后可删除 .env
# 4. 使用 npm run start:secure 启动服务
# 5. 启动时需要输入主密码解密配置
#
# ========================================

# 服务配置
PORT=3000

# 配置数据库连接
CONFIG_DB_HOST=localhost
CONFIG_DB_PORT=3306
CONFIG_DB_USER=root
CONFIG_DB_PASSWORD=your_database_password
CONFIG_DB_NAME=ai-data-platform

# JWT配置（用于用户登录认证）
# 请使用随机字符串，可运行: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-jwt-secret-change-in-production
JWT_EXPIRES_IN=7d

# 文件加密密钥（用于上传文件加密）
# 请使用随机字符串，可运行: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
FILE_ENCRYPTION_KEY=your-file-encryption-key

# 注意：大模型配置已迁移到数据库 sys_ai_configs 表
# 请通过管理后台 AI配置 页面进行管理

# MCP 服务配置
# 用于 skills-server 连接后端 API 的鉴权
# 此配置默认不加密，以便 skills-server 能在无密码交互的情况下读取
MCP_API_KEY=your-mcp-api-key-generate-random

